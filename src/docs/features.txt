========= INFORMATION =========

Updated: 06/02/2026
Updated by: Sadman

------------------------------------------------------------------------------------------------------------------
[A] = Admin Only Feature
FIX (Addresses a specific complaint about EliteEssentials)
> = Command
? = Additional functions
- = Info
[!] = COMPETITOR
✅ = Completed

You can find the features we have to work on below:
------------------------------------------------------------------------------------------------------------------

|| BASIC FEATURES:
-----------------------------
1. QoL:
    > /hf reload : Reloads all configs
    > /hf save [data_type] [target] : Saves a certain type of data, could be of a player
        - /hf save playerdata Raeden
    > /hf defaultLanguage set [language_name]
    > /hf saveinterval set [interval_name] [time]
        - /hf saveinterval set playerdata 5m
    > /hf debug on/off
    > /hf analytics commandexec on/off : Turns on/off tracking for all executed command
    > Regenerate to full health after slee
        ? Can be turned off in configs


|| ADMIN FEATURES:
-----------------------------
    > [A] /god [player] : Grants total invincibility to the player.
    > [A] /heal [player] : Fully restores health.
    > [A] /vanish : Makes the admin completely invisible to other players.
        - [!] COMPETITOR FIX: Ensures player indicators on the map (M) are hidden while vanished.
        - [!] COMPETITOR FIX: Suppresses fake "join/leave" messages to prevent confusion.
    > [A] /invsee [player] : View and edit a player's inventory.
    > [A] /clearinv [player] : Clears a player's inventory.
    > [A] /hideinv [player] : Hides a player's inventory
    > [A] /unhideinv [player] : Unhides a player's inventory
    > [A] /fly [target] : Makes target fly
    > [A] /flyspeed [target] [amount] : Changes flyspeed of the target
    > [A] /pinfo [target] : Shows all player information to admin
        - All stats
        - UUID, Location, Health, Stamina, Gamemode
        - Other data tracked by plugin
    > [A] /freeze : Freezes a player in place, they cannot move or lookaround
    > [A] /unfreeze: Unfreezes a player
    > [A] /extinguish [target] : Removes fire from player if they are on fire
    > [A] /broadcast [msg] : Sends message to all players across different universe (All players in server)
        - [BROADCAST] msg
        ? Configurable target in config (Everyone/World Only)
    > [A] /announce [msg] : Sends message to all players across different universe (All players in server)
            - [ANNOUNCEMENT] msg
            ? Configurable target in config (Everyone/World Only)
    > [A] /title [player] [title] [message] : Sends a title to player
        - /title all : Sends everyone
    > [A] /title create : Open title creation GUI
    > [A] /title test [name] : Test a title
    > [A] /top : Teleport to the highest block at the current x, y coordinate
    > [A] /staffchat [msg] : Sends a message to staff chat
    > [A] /staffchat toggle [on/off] toggles staff chat on and off
    > [A] /jump : Teleports to the block you're looking at
    > [A] /anonymous : Removes prefix and everything from your message in chat for anonymous messaging.


|| ANALYTICAL FEATURES:
-----------------------------
1. Action logs:
    > /actionlog [log_type] [on/off] : Turns a certain action logging on or off
    > /actionlog status : Shows a list of all active and disabled logs



|| CHAT FEATURES:
-----------------------------
1. Message Format:
    > Prefix/Suffix and message color system
    > Nicknaming should be available
    > Color naming should be available
    > Tag creation in game (Suffix / Prefix)
    > ✅ Colored Message Available

2. Managing Chat
    > /clearchat : Removes all messages in chat
    > /clearchat [amount] : Removes last sent X number of messages in chat
    > /chatcontrol censor [on/off] : Toggles chat censor for blocked words and actions
    > /chatcontrol censor add [word] : Censors a word
    > /chatcontrol mutechat [name] : Mutes a whole chat

3. Integrated Features:
    > Remove Cuss/Censored Words
    > Remove Links
    > Remove other server mentions / ips
    > Prevent repeating message
    > Chat formating through config

4. Chat Games/Pranks:
    > Counting Games
    > Math Games
    > Type the word games
    > Prank repeat offenders in chat

|| PLAYER CONVENIENCE FEATURES:
-----------------------------
1. Home system
    > /home [name] : Teleports player to mentioned home
    > /homes : Opens homes GUI (shows player's available homes)
    > [A] /homes admin : Opens list of players to see their created homes
    > /sethome [name]
        ? Configurable CD between setting homes
    > /delhome [name]
    > /home update [name]
        ? Configurable warmup for teleporting to home or between consecutive teleportation
        ? Visual Marker on map for home
        ? Logging: History of created/deleted homes

2. Teleport System
    > /tpa : Opens a list of players they can teleport to
        - Blocked players and players they are blocked by doesn't appear
        - Friends of player appear at top of the list
    > /tpa [name] : Sends a teleport requestion to the person
    > /tpaccept : Accepts the current teleportation request
    > /tpadeny : Denies current teleportation request
    > /tpsettings:
        - Teleport Request mode : [Anyone | Mutuals | None]
        ? Configurable warmup for teleporting to player or between consecutive teleportation
        ? Logging: Total teleportation requests sent

3. Random Teleportation System
    > /rtp : Randomly teleports to wild in configured ranges (Supports per world)
    > [A] /rtp [player] : RTPs a player
    > [A] /rtp [player] [world] : RTPs a player at certain world
    > [A] /rtp toggle [on/off] : Turn this function on or off for players (admins can still use)
        ? Configurable warmup for RTPing and between consecutive teleportation
        ? Logging: Total RTPs executed

4. Social System
    > /friend : Opens friends GUI
    > /friend list : Opens friends GUI
    > /friend location : Shows the location of the friend
    > /friend gift [name] : Gives the item in hand a gift to the mentioned friend
    > /friend add [name] : Sends friend request to player
    > [A] /friend forceadd [name] : Adds the user as friend
    > [A] /friend forceadd [target1] [target2] : Adds two targets as friends
    > /unfriend [name] : Removes a person from friend list
    > /friend remove [name] : Same as unfriend
    > /friend settings : Opens settings menu for friends (Accessible from friends GUI as well)
        - Friend request mode: [Anyone | Mutuals | None]
    > [A] /friend admin : Opens Admin UI containing all players and their friend information (friend settings as well, changable as well)
    > /block [name] : Blocks a player, restricting all private interaction and public interaction from them
    > /unblock [name] : Unblocks a player
    > /blocklist : Opens a GUI of the user's block list
    > [A] /blocklist admin : Opens a GUI with players in side panel and their blocklist
    > /vouch [target name]: Vouches for target affecting their reputation (According to a server's reputation)
    > /unvouch [target name] : Unvouches a target if user already vouched for them.
        ? Configurable max friend per user
        ? Configurable max vouches per user
        ? Configurable max block per user
        ? Logging: Number of times unfriended by others, number of times sent friend req, number of people blocked them

5. Message System:
    > ✅ /msg [name] [text]: Sends a private message to player
    > ✅ /reply [text]: Replies to private message if there is any receiver
    > [A] /mute [name] [duration] : Mutes a player for X time (Duration stacks if muted again)
    > ✅ [A] /unmute [name] : Unmutes a player
    > [A] /unmuteall : Unmutes all muted player in the server
        - Need to save a log for the remaining mute duration for every player unmuted
        - /unmuteall undo : In case this was done by mistake, turns back all previous mute duration
    > /ignore [name] : Blocks all messages from the target (Private messages only)
    > /unignore [name] : Removes the target from ignore list
        ? Configurable max mute duration for play
        ? Logging: Total pm sent, total mutes received, total times ignored, ignored logging, total muted time

6. Mail System:
    > /mailbox : Opens mailbox GUI
    > /mail : Opens GUI to send a mail
    > /mail [target] [text] : Sends a quick mail to the target
    > /mail list : Shows list of mails the player has (read and unread) in chat
        ? Configurable max mails in inbox
        ? Configurable max mails that can be sent per day
        ? Configurable who can mail the person
        ? Mail ID for tracking
        ? Logging: Mails sent by user, Mails received, Mail logs (who sent who and when)

7. Group Chat System:
    > /groupchat : Opens groupchat UI
    > /groupchat join [name] : Joins a public groupchat
    > /groupchat type [open/private] : Sets a groupchat to public or private
    > /groupchat list : Shows list of all public groupchats available to join
    > /groupchat [msg] : Sends a message to groupchat without opening GUI
    > /groupchat [hide/show] : Toggle showing message of groupchat in global chat or not
    > /groupchat create [name] : Creates a groupchat
    > /groupchat disband [name] : Deletes a groupchat
    > /groupchat member [add/remove/promote/mute/unmute] [name] : Manages a player in groupchat
        ? Configurable member limit per group chat
        ? Logging: Messages, setting changes, joins/leaves in public GCs
    > [A] /groupchat delete [name] : Force deletes a user's groupchat
    > [A] /groupchat flag [name] : Flags a groupchat for stricter logging
    > [A] /groupchat mute [name] : Mutes a groupchat

8. Rank System:
    > /rank list
    > /rank chain set [target] [chain_name] : Sets a certain rank chain for target
    > /rank chain create [name] : Opens rank chain creation GUI
    > /rank create : Opens rank creation UI
    > /rank edit [name_of_rank] [property_name] [new_value] : Edits the property of a rank
    > /rankset [player] [rank name]
    > /rankup [player] (promotes player to next rank)
    > /derank [player] (deranks player to last rank)

9. Reporting System:
    > /report - Opens reporting GUI
    ? Logging: Report time, Reported By, Report Type

10. Kit System:
    > /kits : Opens available kit to claim menu
    > /kit creator : Open kit creator GUI
        - Permissions, Adding items, naming, Claimable by, Reclaim delay etc.
    > /kit give [target] : Gives a kit to target
    > /kit

11. QoL Player commands:
    > /playtime : Shows player's current playtime
    > /playtime rewards : Opens playtime rewards menu
        - [!] COMPETITOR FIX: Console commands for rewards are guaranteed to execute (fixes bug where rewards "triggered" but gave nothing).
    > /show players : Shows list of all online players
    > /pinfo [target] : Shows various information of a player:
            - Playtime (Last seen if inactive)
            - Total Deaths
            - Blocks Mined
            - Player Kills
            - Mob Kills
            - Online/Offline status
    > /autosmelt : Smelts ore as you mine them
        - Works as a potion effect for X minutes (unless turned on infinitely)
        ? Configurable CD
    > /cure [player] : Removes all negative status effects of player and provides immunity
        ? Configurable CD
    > /itemlore [lore] : Sets lore of the item held in hand
        ? Configurable CD
    > /itemrename [name] : Sets name of item held in hand
        ? Configurable CD
    > /nick : Change one's nickname
        - [A] /nick [target] : Set a player's nickname
        ? Configurable CD
    > /repair : Repairs the item in hand
        ? Configurable CD
    > /repairall : Repairs all items in inventory
        ? Configurable CD
    > /vault | /backpack : Opens rows of GUI for player where they can store stuff
        - Can be differently named
        ? Configurable sizes for players
        ? Configurable Name
    > /smelt : Smelts all item in hand if smeltable
        ? Configurable CD
    > /smeltall : Smelts all item in inventory if smeltable
        ? Configurable CD
    > /furniture : Portable furniture workbench
        ? Configurable CD
    > /anvil : Portable Blacksmith anvil
        ? Configurable CD
        ? Levels can vary
    > /alchemy : Portable Alchemist's workbench
        ? Configurable CD
        ? Levels can vary
    > /armor : Portable Armorer's Workbench
        ? Configurable CD
        ? Levels can vary
    > /stove : Portable Chef's stove
        ? Configurable CD
    > /farm : Portable Farmer's Workbench
        ? Configurable CD
        ? Levels can vary
    > /workbench : Portable Workbench
        ? Configurable CD
    > /back : Teleports player to their previously visited location (Or death location)
        - Saves multiple location besides death but prioritizes deaths
        ? Configurable number of times player can use /back consecutively
        ? Configurable warmup
        ? Configurable CD between teleportation
    > [A] /sleepercent [value] : Skip nights based on % of player sleeping


|| ECONOMY FEATURES:
-----------------------------
1. Multiple Currency Support:
    > /balance : Check your current wallet balance.
    > /baltop : Displays the richest players on the server.
    > /pay [player] [amount] : Transfer money to another player.
    > [A] /eco [give/take/set] [player] [amount] : Admin management of player balances.
    > [A] /eco balances : Admin GUI to check balances of all players (online and offline)
    > [A] /eco track [player] : Tracks the income stream of the player (All transactions and money additions)
    ? Logging: Transactions of players, per-player income tracking

 2. Command Cost system:
    > /eco commandcost list : Opens a GUI of list of all commands and their cost to execute
    > /eco commandcost set [command_name] [amount] : Setting command cost instead of through configs

|| SERVER MANAGEMENT FEATURES:
-----------------------------
1. Warp System:
    > /warp : Opens GUI to show available warps to that user
        - Admins will have management tools as well
    > /warp [name] : Teleports player to the destination of warp
    > /warp list : Shows list of available warps in chat
    > [A] /warp create [name] : Create a warp at the location
    > [A] /warp delete [name]
    > [A] /warp setdesc [text] : Sets the description of the warp
    > [A] /warp setperm [perm node] : Sets the permission to warp to destination
    > [A] /warp whitelist [add/remove/list/on/off] : A more complex feature, doesn't allow anyone to teleport to warp unless they are admin
                                                     even if they have the specific permission for the warp.
    > [A] /warp info [name] (Location, Time created, Created by, Total Lifetime, Total Warps, Warp Permission, Warp Description)

2. Spawn System
    > /spawn : Teleport to server spawn.
    > [A] /setspawn : Sets the main server spawn point.
        - [!] COMPETITOR FIX: Config ensures spawn location is not lost/reset after plugin updates.
        - [!] COMPETITOR FIX: Respawn logic forces players to this spawn point if no bed is found (fixes default game spawn bug).
    > [A] /spawnprotect [radius] : Sets protection zone around spawn.
        - [!] COMPETITOR FIX: Protection extends to entities/paintings/frames, not just blocks.

3. Maintenance:
    > [A] /maintenance [on/off] : Toggles whitelist and kicks non-staff members.
    > [A] /whitelist : Opens whitelist GUI for various settings regarding whitelist

4. Automated Command Executions:
    > On Player Server Join
    > On First Server Join
    > On Quit Server
    > On Player Death
    > On Player Respawn
    > On Player Teleport
    > On Player Change World
    > On Player Disconnect
    > Certain number of online players
        ? Can add more functions through configs
    > Timed
        ? Everything should be configurable

5. Command & Alias Management:
    > /alias create [alias] [command] : Creates a custom shortcut for a command.
        - [!] COMPETITOR FIX: Aliases are stored in a persistent database that does not wipe/break during plugin updates.
    > /alias delete [alias] : Removes a custom alias. /alias list : Shows all active custom aliases.
    > /commandcost set [command] [cost] : Sets a price for using specific commands (e.g., /home costs $50).
        - [!] COMPETITOR FIX: Allows different costs based on permission groups (e.g., VIPs pay less).

6. MOTD System:
    > /motd
        ? Configurable in configs

7. QoL:
    > /rules : Shows the server's rules
        ? Per world rule setting
        ? Multiple rule strings
        ? Configurable
    > /discord : Shows the server's discord information

|| MIGRATION/INTEGRATION:
-----------------------------
> Luckperms Integration
> Vault unlocked compatibility for economy
> /migrate : to execute migration [Is a console command]
Make sure users can migrate from other plugins such as:
    1. EliteEssentials
    2. HomesPlus
    3. PrefixesPlus
    4. AdminUI
    5. Hyseentials
    6. KitPlus
    7. Essentials Plus
    8. HyEssentialsX
    9. TheEconomy
    10. Coins

------------------------------------------------------------------------------------------------------------------